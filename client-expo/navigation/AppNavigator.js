import React from "react";
import { NavigationContainer } from "@react-navigation/native";

import { RadioNavigator } from "./RadioNavigator";

export default AppNavigator = props => {
  return (
    <NavigationContainer>
      <RadioNavigator />
    </NavigationContainer>
  );
};
