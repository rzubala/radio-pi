class Track {
    id: string
    url: string
    logoUrl: string
    constructor(id: string, url: string, logoUrl: string) {
        this.id = id
        this.url = url
        this.logoUrl = logoUrl
    }
}

export default Track